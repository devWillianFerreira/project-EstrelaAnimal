import Hamburger from "hamburger-react";

const HamburgerMenu = () => {
  // const [Open, setOpen] = useState(false);
  return (
    <div>
      <Hamburger size={10} />
    </div>
  );
};

export default HamburgerMenu;
